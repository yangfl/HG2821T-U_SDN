#Copyright (C) 2001 Erik Andersen <andersen@codepoet.org>
#
# Note: BusyBox init doesn't support runlevels.  The runlevels field is
# completely ignored by BusyBox init. If you want runlevels, use
# sysvinit.
#
# Format for each entry: <id>:<runlevels>:<action>:<process>
#
# id        == tty to run on, or empty for /dev/console
# runlevels == ignored
# action    == one of sysinit, respawn, askfirst, wait, and once
# process   == program to run

# Startup the system
null::sysinit:/bin/mount -t ubifs ubi0:Rootfs -o sync,remount,ro /
null::sysinit:/bin/mount -t proc proc /proc
null::sysinit:/bin/mkdir -p /dev/pts
null::sysinit:/bin/mount -a
null::sysinit:/bin/hostname -F /etc/hostname
null::sysinit:/bin/ifconfig lo 127.0.0.1 up
null::sysinit:/bin/route add -net 127.0.0.0 netmask 255.0.0.0 lo
null::sysinit:/bin/cp /etc/passwd_old /var/passwd
# now run any rc scripts
::sysinit:/etc/init.d/rcS
# Put a getty on the serial port
#ttyS0::respawn:/bin/getty -L ttyS0 115200 vt100 # GENERIC_SERIAL
#ttyS0::respawn:-/bin/sh
# Logging junk
null::sysinit:/bin/touch /var/log/messages
#null::respawn:/bin/syslogd -n -m 0
#null::respawn:/bin/klogd -n

# Stuff to do for the 3-finger salute
::ctrlaltdel:/bin/reboot

# Stuff to do before rebooting
null::shutdown:/bin/ifconfig wlan0 down
null::shutdown:/bin/killall klogd
null::shutdown:/bin/killall syslogd
null::shutdown:/bin/umount -a -r
null::shutdown:/bin/swapoff -a
